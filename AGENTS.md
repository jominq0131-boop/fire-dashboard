# FIRE Dashboard AI 협업 규칙

- 요청 범위 밖 파일은 수정하지 않는다.
- 실제 금융 데이터, 백업, 비밀값을 코드·테스트·커밋에 포함하지 않는다.
- 새 의존성은 목적, 대안, 유지 비용을 설명한 뒤 추가한다.
- 데이터 스키마 변경에는 결정론적 마이그레이션, 테스트, 문서를 함께 작성한다.
- 테스트를 삭제하거나 약화하여 통과시키지 않는다.
- 금액·날짜·FIRE 계산 변경에는 관련 테스트를 추가한다.
- 대규모 재작성, 동기화 구조 변경, 외부 서비스 도입 전에는 명시적 승인을 받는다.
- 작은 단위로 구현하고, 변경 이유와 검증 결과를 PR에 기록한다.
- 작업 시작 시 `docs/project-continuity.md`와 관련 설계·테스트 문서를 읽고 GitHub 최신 상태를 확인한다.
- 모든 작업에서 문서 영향도를 확인하고, 영향을 받은 문서를 같은 작업/PR에서 반드시 갱신한다. 기능·진행 상태는 `README.md`와 `docs/project-continuity.md`, 설계는 `docs/architecture.md`, 스키마·마이그레이션은 `docs/data-model.md`, 검증은 `docs/testing.md`, 계층 책임은 해당 `src/*/README.md`에 반영한다.
- 운영 정책 변경은 이 파일과 연속성 문서에 함께 반영하고, 작업별 변경 이유·검증 결과·미완료 사항은 `docs/work-log.md`와 PR에 기록한다. 영향 없는 문서는 불필요하게 수정하지 않는다.
- 로컬 구현·검증, PR 검토 중, main 병합, 실제 배포 완료를 구분해서 기록한다. 테스트 실행 전이나 배포 확인 전에 완료했다고 적지 않는다.
- 단기 브랜치는 PR 병합 여부와 현재 tip이 병합 당시 head와 일치하는지 확인한 뒤 삭제한다. main, 열린 PR, 미병합 변경은 보존한다.
