# 작업 기록

## Milestone 15 — Issue #41

2026-09-05, 원격 main d3a5e0f와 열린 PR/issue 없음, 해당 main CI/Pages 성공을 확인하고 codex/goal-based-fire에서 구현했습니다. 배포 작업을 위해 [Issue #41](https://github.com/jominq0131-boop/fire-dashboard/issues/41)을 만들었습니다. 현재 현금/주식과 저축 추세에서 목표 도달월을 찾고 도달시점 자산 구성, 주식의 연3% 세전 자가배당을 표시해 달라는 요청입니다.

GoalPlanner와 goal-fire 순수 계산을 추가했습니다. 기존 제한 조회의 완료월 평균을 사용하고, 현금/NISA 적립/NISA 성장/과세계좌를 분리합니다. NISA는 취득액 기준 총량과 두 연간 한도, 성장 부분한도,1월 초기화를 적용합니다. 도달월 구성 막대/금액/비율과6개 시리즈 차트, 월/연 인출액을 표시합니다. 미도달·현금 부족·범위 초과를 명시합니다.

신규 입력은 화면 메모리만 사용하며 이를 UI와 문서에 명시했습니다. 기존 FIRE 자동 저장/비교는 유지합니다. DB v4/JSON v3, 의존성 변경은 없습니다. 실제 금융 데이터/사용자 프로필을 읽거나 수정하지 않았습니다. 신규 입력 영속 저장, 세후 계산, 도달 후 지속성은 후속 범위입니다.

로컬 단위144개, 신규/기존 FIRE 브라우저8개, lint/typecheck/build/format/diff 검사를 통과했습니다. 합성 데이터로 도달월/인출액/완료월 평균과320/390/1440px 가로 넘침 없음을 확인했습니다. 전체 Chromium 회귀51개도2.7분에 정상 종료/exit0으로 통과했습니다. PR 생성/원격 게시/병합/배포는 아직 수행하지 않았습니다.

PR #42의 첫 CI는50/51개 통과 후 Linux headless의 소수 픽셀 반올림으로 모바일 차트 높이가299.9999px로 측정되어 기존300px 하한 검사1개가 실패했습니다. 테스트를 낮추지 않고 모바일 차트 CSS 높이에1px 여유를 두어 실제 측정값도300px 이상이 되도록 수정했습니다.

## Milestone 14 — Issue #39

시작 main c4fdf6b, 브랜치 feat/persistent-fire-plan. 사용자는 작은 일본어의 가독성을 보강하고 남은 핵심 기능을 빠르게 완성해 MVP를 만든 뒤 개선하기를 요청했습니다.

IndexedDB v4에 단일 firePlans 저장소를 추가하고 입력 문자열, 마지막 계산 가정, 최대3개 비교를 자동 저장합니다. 빠른 입력은 직렬 큐에서 최신 상태만 저장하고 백업 복원은 대기 중 저장 이후 다시 읽습니다. 다른 탭의 변경과 손상·초과 데이터는 덮어쓰거나 삭제하지 않습니다. 투영 결과 점은 저장하지 않고 가정에서 다시 계산합니다. JSON v3은 계획을 포함하며 v1/v2는 계획 없음으로 정규화합니다. 기존 계획과 다른 계획의 가져오기는 금융 기록까지 포함해 전체 취소합니다.

브랜드 타입 규칙은 일본어 표시 문구와 모바일 탭을 계산 크기12px 이상으로 올렸습니다. 단위125개, build, DB v3→v4·FIRE 저장·자원 관련 브라우저5개, 브랜드3개가 통과했습니다. 전체48개에서는47개 통과 후 새 FIRE 오류와 기존 전역 alert 선택자가 겹친1개를 계좌 영역으로 한정했고 해당 자원 회귀를 다시 통과했습니다. 자동 저장·새로고침·다른 브라우저 백업 복원, 계획 충돌,320px 레이아웃과 일본어 글자 크기는 합성 데이터로 검증했습니다. 최종 전체 PR CI, main, Pages와 공개 파일 근거는 Issue #39 연결 PR에 기록합니다.

## Milestone 13 — Issue #37

시작 main b4c01a6, 브랜치 feat/branded-app-shell. 사용자는 MVP라도 엑셀형 업무 도구처럼 보이지 않고 토스·iPhone 앱처럼 세련되어야 하며 폰트·파비콘과 구조 개선까지 요청했습니다.

상승 흐름 브랜드 마크를 앱·SVG 파비콘·maskable 앱 아이콘·180px Apple touch icon에 통일하고 일본어 제품 설명, 테마 색, standalone manifest를 추가했습니다. 외부 폰트 요청 없이 OS 네이티브 sans/rounded 계열과 숫자 정렬을 사용하며 작은 글자, 제목 위계, 카드 깊이, 버튼/입력/포커스 반응을 brand.css로 분리했습니다.860px 이하는 기존 상단 다행 메뉴 대신 safe-area를 고려한5탭 하단 앱 바를 사용하고 reduced-motion을 반영합니다. 금융 계산·IndexedDB·JSON 스키마는 변경하지 않았습니다.

신규/관련 브라우저5개가 통과했습니다. 전체45개 실행에서는44개가 통과하고 시각 헤드라인 변경으로 기존 제품 제목의 접근성 이름1개가 실패했습니다. 헤딩의 접근성 이름을 보존한 뒤 브랜드3개와 해당 기존 검사1개를 다시 실행해4개 모두 통과했습니다. 메타데이터와 공개 자산 응답, PNG 크기, 모바일 고정 위치·5열·아이콘·글자 크기·가로 넘침, reduced-motion, 기존 탐색과 연결 그래프를 검사했습니다. 데스크톱 및390px 전체 화면을 합성 데이터로 직접 확인했습니다. 최종 PR CI에서 전체45개를 다시 실행하고 main CI, Pages와 공개 해시는 연결 PR에 기록합니다.

첫 PR CI는44/45개 통과 후 Pages용 base path가 들어간 manifest URL을 개발 서버에서 요청해 fallback HTML을 JSON으로 읽은 검사 자체의 환경 차이로 실패했습니다. 정적 파일명을 테스트 서버 기준으로 정규화하고 HTTP 성공뿐 아니라 SVG·PNG·manifest Content-Type을 각각 확인해 fallback HTML이 통과하지 못하도록 강화했습니다.

## Milestone 12 — Issue #35

시작 main232496d, 브랜치 feat/connected-trends-unified-design. 사용자는 여러 값이 있어도 점이 떨어져 보이는 문제와 차트만 다크인 불일치를 지적하고 전체 품질 개선·배포를 요청했습니다.

기존 실선/면의 비교 가능한 구간은 보존하며 observationBridges로 실제 관측점을 점선 참고선으로 연결했습니다. 중간 값/계산/DB 변경은 없습니다. 막대 시리즈에도 추세선을 겹치고, 투자 부분 잔액의 계좌 구성 변화도 점선으로 구별합니다. 단일 관측 범위에는 설명을 제공합니다. theme.css 공통 토큰으로 배경·내비게이션·카드·폼·표·차트·빈 상태·성공/오류·FIRE·백업을 통일했습니다. 향후 전체 화면/다월 검증 기준을 AGENTS와 연속성 문서에 반영했습니다.

단위115개, 신규/관련 브라우저8개 및 이후 최종 Chromium42개 전체 통과(2.1분). 4개 불연속 관측의 상승/하락과 실제 SVG 연결 경로, 중간 월 미기록, 최대 금액·구성 변화 참고선,320px/1440px를 검증했습니다. 기존 실선 분리 검사는 삭제하지 않고 실선/점선 계약을 각각 검사하도록 확장했습니다. 데스크톱 전체 개요/계좌/월별 입력/FIRE, 모바일 백업/연결 차트를 직접 확인했습니다. 실제 금융 기록/사용자 프로필은 사용하지 않았습니다.

로컬 구현·검증과 실제 배포는 별개입니다. 최종 포맷/빌드·PR CI·main CI·Pages 및 공개 파일 해시는 Issue #35 연결 PR의 릴리스 기록에서 확인합니다.

## Milestone 11 — Issue #33

시작 main 0d49f27, 브랜치 feat/professional-chart-workspace. 사용자가 financial-os 수준을 참고로 프로 서비스 품질의 개선을 요청했습니다. Recharts 3.10.0을 고정 도입하고 차트 구현을 별도 동적 청크로 분리했습니다. 자산 면적/투자 부분 잔액 막대, 수입/지출/납입 비교, 상세 툴팁, 시작·종료 확대, 같은 색의 FIRE 자산/목표 및 첫 도달 요약을 구현했습니다. 데이터 스키마와 기존 금융 계산식은 변경하지 않았습니다.

단위114개, format/lint/typecheck/Pages 빌드 통과. 브라우저40개 실행에서38개 통과 후, 로딩 상태 역할 중복과 빈 상태 HTML 변경에 따른2개 실패를 수정했습니다. 이후 해당2개와 신규/관련3개를 집중 재검증해5개 모두 통과했고 변경 영향 없는 결과는 재사용합니다. 최종 PR/main CI는41개 전체를 실행합니다. 합성 데이터의 desktop 복합 차트와 tooltip,320px 터치 tooltip을 직접 확인했습니다. 리사이즈 제한 지연을 제거해 고정 시계 환경에서도 너비를 즉시 갱신합니다. SVG 교체에 따른 기존 테스트 선택자는 실제 선·면 시리즈와 HTML 빈 상태로 바꾸고 기존 검증 의미를 유지했습니다.

차트 다운로드 실패는 경계 내부로 격리하며 계좌 저장이 유지됨을 검증했습니다. npm 설치 감사는 취약점0건, Pages 빌드는 기본 JS255.91kB/gzip80.89kB + 차트 JS395.83kB/gzip113.25kB, CSS27.58kB/gzip6.66kB입니다. 실제 데이터/계정 설정/전역 환경은 사용하지 않았습니다. 공개 산출물은 dist의 HTML/JS/CSS만 포함합니다.

로컬 구현·검증과 병합·배포는 구분합니다. 최종 결과는 이 항목과 Issue #33 연결 PR 릴리스 댓글에 기록합니다.

첫 PR CI에서40/41개 통과 후 viewport 변경 직후 가로 넘침이 발견됐습니다. ResizeObserver의 새 크기 반영 전에도 기존 SVG/래퍼가 부모 너비를 넘지 않도록 max-width를 적용했습니다. 즉시 검사 조건을 유지한 채 해당 테스트를3회 반복해 모두 통과했고 Pages 빌드/format/diff를 확인했습니다.

두 번째 CI에서 너비 제한이 Recharts의 폭0 내부 측정 컨테이너를 기준으로 적용되어 포인터 동작2개가 실패한 것을 재현했습니다. max-width 대신 실제 표시 영역에만 가로 clip을 적용해 측정 좌표를 보존하고 세로 툴팁은 유지합니다. 터치 툴팁 전체 너비가 표시 영역 안에 있는지도 추가 검증했습니다. 포인터·터치·즉시 리사이즈3개 시나리오를2회씩 함께 반복해6개 모두 통과(29.2초)했고 Pages 빌드/format/diff를 확인했습니다.

## Milestone 10 — Issue #31

사용자가 문서 순서 최신화와 상호작용 가능한 선 그래프, 기록에서 확인/예측까지 이어지는 기능 및 배포를 요청했습니다. 시작 main은8a92933, 브랜치는 feat/interactive-financial-charts입니다.

월별 선택·상세·필터·예측 인계, 자산/목표/비교 선 차트와 범례·기간·키보드 조작을 구현했습니다. 저장 형식·금융 계산식·의존성은 변경하지 않았습니다. 현재 문서는 사용/개발 순서로 다시 구성하고 이전 원문은 history에 보존했습니다.

로컬 최종 검증은 단위113개·Chromium38개(1.1분), format/lint/typecheck/Pages build/diff 검사 통과입니다. 문서31개 상대 링크에 오류가 없고, 원문11개의 본문이 보관본에 유지됨을 이전 main과 대조했습니다. 데스크톱 비교 차트·모바일 기록 선택 화면을 합성 데이터로 확인했습니다.

초기 새 테스트의 select 라벨 탐색 문제는 명시적 접근성 이름으로 수정했습니다. 화면 검토에서 현금흐름 누락을 찾아 월 인덱스로 최대12건을 같은 읽기 트랜잭션에서 검증·조회하도록 보강했습니다. 기존 제한 조회 테스트의 기대 요청을 잔액1200 + 현금흐름12로 정확히 갱신했고, 실제 저장값/미입력/손상/합계overflow를 추가 검증했습니다. 계산식과 DB/JSON 버전, 의존성은 그대로입니다.

이 기록은 로컬 검증 시점이며 PR/main 병합·배포는 이후 단계입니다. 최종 SHA·Actions·공개 파일 검증 결과는 [Issue #31](https://github.com/jominq0131-boop/fire-dashboard/issues/31) 연결 PR의 릴리스 기록을 따릅니다. 과거 모든 작업 원문은 [이전 작업 기록](history/work-log.md)에 있습니다.
